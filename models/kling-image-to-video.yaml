schema: neta.generation.model.v1
model: kling-image-to-video
title: Kling Image To Video
category: video
description: Animates a reference image into a short video. It has the same cost as Kling text-to-video and more permissive content moderation than Seedance.
adapter:
  type: kling.videoGenerations
content:
  input:
    - type: text
      required: true
      min: 1
      max: 16
      merge: newline
      description: Video prompt.
    - type: image
      required: false
      max: 2
      sources:
        - url
      description: First frame and optional tail frame image input. Provider-native image input may be passed in meta.
parameters:
  duration:
    type: integer
    optional: true
    default: 5
    min: 5
    max: 10
    description: Video duration in seconds.
  aspect_ratio:
    type: string
    optional: true
    default: 16:9
    enum:
      - 16:9
      - 9:16
      - 1:1
    description: Output aspect ratio.
  mode:
    type: string
    optional: true
    default: std
    enum:
      - std
      - pro
    description: Kling generation mode.
  cfg_scale:
    type: number
    optional: true
    default: 0.5
    min: 0
    max: 1
    description: Prompt adherence scale.
  poll_interval:
    type: integer
    optional: true
    default: 5
    min: 1
    max: 30
    description: Seconds between task status checks.
  max_wait:
    type: integer
    optional: true
    default: 900
    min: 30
    max: 1800
    description: Maximum seconds to wait for task completion.
  negative_prompt:
    type: string
    optional: true
    description: Negative prompt.
  seed:
    type: integer
    optional: true
    description: Random seed for reproducibility.
examples:
  - title: Image to video
    request:
      model: kling-image-to-video
      content:
        - type: text
          text: gently turn toward the camera with soft natural motion
        - type: image
          source:
            type: url
            url: https://example.com/input.png
      parameters:
        duration: 5
        aspect_ratio: 16:9
